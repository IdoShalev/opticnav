# set the team environment
# in ~/.bashrc, add "source /cs/trunk/team/env.sh" at the end (without quotes)
# echo "source /cs/trunk/team/env.sh" >> ~/.bashrc

DIR=$(dirname $BASH_SOURCE)

export PATH=$DIR:$PATH

